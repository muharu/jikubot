export interface GuildCardProps {
  id: string;
  name: string;
  icon?: string | null;
  isJoined: boolean;
}

export interface CardContentProps {
  id: string;
  name: string;
  isJoined: boolean;
}
